import React from 'react';
import './App.css';

function App() {
  // useState to store the pokemon list and the favorites
  // useEffect queries the api when the component mounts, [] at end to limit to one query

  return (
    <div className="App">
      <h1>Listening!</h1>
    </div>
  );
}

export default App;
